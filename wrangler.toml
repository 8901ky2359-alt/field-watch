name = "field-watch"
main = "worker/index.js"
compatibility_date = "2024-09-23"

# Fully static export (Next.js `output: 'export'`, built from webapp/) —
# no D1/R2 bindings needed, this app has no backend.
[assets]
directory = "webapp/out"
binding = "ASSETS"
